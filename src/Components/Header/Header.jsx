import React from 'react';
import scss from './Header.module.scss';

const Header = (props) => {
    return(
        <div>
            <strong>Header</strong>
        </div>
    );
}

export default Header;
